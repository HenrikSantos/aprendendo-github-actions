import { render, screen } from '@testing-library/react'
import Page01 from '@/pages/page-01'

describe('Page01', () => {
  it('renders a page', () => {
    // render(<Page01 />)

    // const heading = screen.getByRole('heading', {
    //   name: /this is the page 01/i
    // })

    // expect(heading).toBeInTheDocument()
  })
})
